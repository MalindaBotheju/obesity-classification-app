// frontend/config.js
const CONFIG = {
    // For local development:
    // API_URL: "http://localhost:8000"
    
    // For production (Render), you would comment out the local line and use this:
    API_URL: "https://obesity-classification-app-z9sy.onrender.com"
};